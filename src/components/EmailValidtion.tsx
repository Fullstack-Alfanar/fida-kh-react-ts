import React from "react";
import { FC } from 'react';

interface EmailValidtionProps{
    email:string;
  
}

const EmailValidtion =({email}:EmailValidtionProps)=> {
    console.log(email);


    
    return(
       <></>
    );


};
export default EmailValidtion;